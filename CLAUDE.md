# Claude Code Notes

Read `AGENTS.md` first.

The shared memory for this project is:

- `docs/PROJECT_MEMORY.md`

When you finish a meaningful task, append or update a short entry there so Codex and Claude Code stay aligned.

Use `docs/PROJECT_MEMORY.md` as the coordination channel:

- Read "Active Backlog" before choosing work.
- Add or update a task note before making large changes.
- Record files touched and verification results when handing off.
